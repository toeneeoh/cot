OnInit.final("Colosseum", function(Require)
    ITEM_LOOKUP[FourCC('I0ER')] = function(p, pid)

        -- TODO: Check for ticket, prompt all users via custom frame to accept or deny joining colosseum
        -- Difficulty will scale by player level and relative strength (?)
    end
end, Debug and Debug.getLine())
